#include <bits/stdc++.h>
#define debug(x) cout << #x << " = " << x << endl
#define REP(i,n) for(ll i = 0; i < n; i++)
#define pb push_back
#define ms(a,valor,n) REP(i,n) a[i] = valor;
#define FAST ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0)
#define RANDOM mt19937 rng(chrono::steady_clock::now().time_since_epoch().count())
#define unif(a,b) uniform_int_distribution<int>(a,b)(rng)

using namespace std;

typedef long long ll;

void snum(ll &x) { scanf("%lld",&x);}
void snum(ll &x, ll &y){ scanf("%lld %lld",&x,&y);}
void snum(ll &x, ll &y, ll &z){ scanf("%lld %lld %lld",&x,&y,&z);}

void doit(){

}

int main() {

	ll T = 1;
	//snum(T);
	
	while(T--){
		doit();
	}


	return 0;
}


